import { DobToAgePipePipe } from './dob-to-age.pipe';

describe('DobToAgePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DobToAgePipePipe();
    expect(pipe).toBeTruthy();
  });
});
