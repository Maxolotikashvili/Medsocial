import { ScrollFromTop } from './scroll-from-top.directive';

describe('ScrollFromTop', () => {
  it('should create an instance', () => {
    const directive = new ScrollFromTop();
    expect(directive).toBeTruthy();
  });
});
