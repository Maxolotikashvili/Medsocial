import { ScrollToBottom } from './scroll-to-bottom.directive';

describe('ScrollToBottom', () => {
  it('should create an instance', () => {
    const directive = new ScrollToBottom();
    expect(directive).toBeTruthy();
  });
});
