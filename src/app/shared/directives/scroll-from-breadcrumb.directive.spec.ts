import { ScrollFromBreadcrumbDirective } from './scroll-from-breadcrumb.directive';

describe('ScrollFromBreadcrumbDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollFromBreadcrumbDirective();
    expect(directive).toBeTruthy();
  });
});
